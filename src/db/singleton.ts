/**
 * Feste UUID für Tabellen mit genau einer Zeile (Halterangaben,
 * Einstellungen). Ein zweites Gerät meint bei der späteren Synchronisation so
 * dieselbe Zeile, statt eine zweite anzulegen.
 */
export const SINGLETON_ID = '00000000-0000-4000-8000-000000000001';
